swagger: '2.0'
info:
  title: Mouth Metrics API
  description: API for the Mouth Metrics service.
  version: 1.0.0
schemes:
  - https
produces:
  - application/json
paths:
  /:
    get:
      summary: Get the homepage.
      operationId: root
      x-google-backend:
        address: ${run_service_url}
        jwt_audience: ${run_service_url}
      responses:
        '200':
          description: A successful response
          schema:
            type: string
